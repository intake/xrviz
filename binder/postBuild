#!/bin/bash

# Install the dependencies
conda install -c conda-forge -y python=3.7 geoviews metpy --file ./requirements.txt

# Install xrviz from current branch
pip install . --no-deps

# Get rid of source code
rm -rf xrviz
rm -rf docs

# Get into examples
cd examples
